import React from 'react';
/*arrow anonimus es6 function that has no name*/
const Intro = props => (
  <p className="the description of the list">The list received from api response
  </p>
);

export default Intro;
